<Page
    x:Class="TestAppSysTech.Pages.UserEditPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TestAppSysTech"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">


    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock Text="Управление персоналом  " Foreground="#0091d0" Margin="20" Style="{ThemeResource HeaderTextBlockStyle}"/>

        <ListView x:Name="personsList" Grid.Row="1" Margin="10">
            <ListView.ItemTemplate>
                <DataTemplate  x:DataType="local:Person">
                    <StackPanel>
                        <TextBlock FontSize="16" Text="{x:Bind Name}" FontWeight="Bold"  />
                        <TextBlock FontSize="16" Text="{x:Bind Group.Name}" />
                        <TextBlock FontSize="16" Text="{x:Bind DateOfStart}" />
                        <TextBlock FontSize="16" Text="{x:Bind BaseSalary}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        
    </Grid>
</Page>
